#include <stdio.h>

int main(void)
{
	int num, iterator;

	iterator = 1;
	num = 32;

	while(break == 0)
	{

		if (num % iterador == 0)
		{
			a = num % iterador;
			iterator = 2;
			printf("%d");

			if(num <= 2)
			{
				break = 1;
			}
		}
		else
		{
			iterator ++;
		}
	}
}
