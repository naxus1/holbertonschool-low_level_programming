#include "holberton.h"

void print_most_numbers(void)
{
	int iterator;

	for (iterator = '0'; iterator < '10'; iterator++)
	{
		if (iterator != '2' || iterator != '4' )
		{
			_putchar(iterator);
		}
	}
}
