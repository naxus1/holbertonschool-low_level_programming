#include "lists.h"


dlistint_t *insert_dnodeint_at_index(dlistint_t **h, unsigned int idx, int n)
{
	dlistint_t *aux_nod, *new_nodo;
	aux_nodo = *head;

	new_nodo = malloc(sizeof(dlistint_t));
	if (new_nodo == NULL)
		free(new);
	 
	
}
