#include "holberton.h"

void reverse_array(int *a, int n)
{
	int arrrev[n], j, i;

	j = n - 1;

	for (i = 0 ; i < n; i++, j--)
	{
		arrrev[j] = a[i];
	}

	for (i = 0 ; i < n; i++)
	{
		a[i] = arrrev[i];
	}

	return (a);
}
