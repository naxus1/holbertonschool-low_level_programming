#include "holberton.h"

int main(void)
{
	int number, sum;

	sum = 0;

	for(number = 0; number < 10; number++)
	{
		if(number % 3 == 0 || number % 5 == 0)
		{
			sum = sum + number;
		}
	}

	printf ("%d", sum);

	return (0);
}
