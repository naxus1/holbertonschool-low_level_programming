#include "holberton.h"

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int print_sign(int n)
{

	int sign;

	if (n > 0)
	{
		sign = 1;
	}

	else if (n == 0)
	{
		sign = 0;
	}

	else if (n < 0)
	{
		sign = -1
	}

	return(sing);
}
