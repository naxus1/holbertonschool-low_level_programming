#include "holberton.h"

void times_table(void)
{
	int rep, num;

	for(rep = 0; rep < 9; rep++)
	{
		for (num = 0; num <= 9; num++)
		{
			_putchar( (9*num) + '0');
			_putchar(',');
		}

		_putchar('\n');
	}

}
