#include "holberton.h"

void puts2(char *str)
{
	int crash, contador;

	crash = 0;
	contador = 0;

	while (crash == 0)
	{
		if (*s != '\0')
		{
			contador++;
			s++;
		}
		else
			crash = 1;
	}

	for (i = 0; i < contador; i+=2)
	{
		_putchar('-');
	}
}
