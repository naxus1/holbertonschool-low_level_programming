#include <stdio.h>
/**
* main - Entry point
*
* This function return the alphabet
* Return: Always 0 (Success)
*/
int main(void)
{
char alph = 'a';
char new_line = '\n';

while (alph <= 'z')
{
putchar(alph);
c++;
}
putchar(new_line);
return (0);
}
